---
import SectionHeader from '@components/ui/section-header.astro';
import Sticker from '@components/ui/sticker.astro';
import FileLink from '@components/ui/file-link.astro';
import EvidenceCard from '@components/sections/pika/evidence-card.astro';
import raw from '@data/ads.json';
import type { AdsContent } from '@content-types/content';
import Decor from '@components/ui/decor.astro';

const ads = raw as AdsContent;
---

<section id={ads.meta.id} class="section" aria-labelledby="ads-title" data-reveal-group>
  <Decor src="el-megaphone.png" w={190} top="5%" right="4%" rotate="-10deg" opacity={0.4} parallax={0.09} />
  <Decor src="el-exclamation.png" w={52} bottom="10%" left="3%" rotate="-14deg" opacity={0.45} parallax={-0.07} hideMobile />
  <Decor src="el-star-rough.png" w={85} top="24%" right="8%" rotate="-12deg" opacity={0.4} parallax={0.07} hideMobile />
  <Decor src="el-cat.png" w={90} top="64%" right="7%" rotate="10deg" opacity={0.42} parallax={-0.07} hideMobile />
  <div class="container">
    <SectionHeader {...ads.meta} />

    <p class="intro" data-reveal>{ads.intro}</p>

    <div class="split">
      <ul class="approach" data-reveal-group>
        {ads.approach.map((a, i) => (
          <li data-reveal>
            <Sticker label={a.label} tilt={((i % 4) + 1) as 1 | 2 | 3 | 4} />
            <p>{a.note}</p>
          </li>
        ))}
      </ul>

      <div class="evidence" data-reveal-group>
        {ads.evidence.map((e) => <EvidenceCard evidence={e} tilt={2} />)}
      </div>
    </div>

    <div class="flow-tight" data-reveal>
      <FileLink {...ads.file} />
    </div>
  </div>
</section>

<style>
  .intro {
    max-width: 68ch;
    font-size: 1.125rem;
  }

  .split {
    display: grid;
    grid-template-columns: minmax(0, 4fr) minmax(0, 6fr);
    align-items: start;
    gap: var(--gap);
  }

  .approach {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .approach li {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: .4rem;
    min-width: 0;
  }

  .approach :global(.sticker) {
    white-space: normal;
  }

  @media (max-width: 899px) {
    .split {
      grid-template-columns: 1fr;
    }
  }
</style>
